import express from "express";
import ReviewsCtrl from "./reviews.controller.js"

const router = express.Router();

// The router variable will help route the user to different parts of the application.

// router.route("/").get((req, res) => res.send("Hello World"));
// Here the "/" means we are using the base route. 

// The above router code is for base/deafult route. Now new code is put to
router


export default router;
